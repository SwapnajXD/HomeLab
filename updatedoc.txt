Here’s a **clean, audit-style change report** of everything that was modified, validated, and improved in your telemetry stack during this session.

---

# 📊 HOMELAB TELEMETRY CHANGE REPORT (ATHENA)

## 📅 Date

June 4, 2026

## 🖥️ Host

Athena (Ubuntu VM in Proxmox)

---

# 1. 🧠 OVERALL OBJECTIVE

The goal of this change cycle was to:

* Stabilize Loki (fix readiness/health confusion)
* Modernize log pipeline
* Introduce Grafana Alloy as unified telemetry agent
* Verify full observability stack (metrics + logs)
* Ensure Grafana query readiness for visualization/screenshot

---

# 2. 🏗️ ARCHITECTURE TRANSFORMATION

## BEFORE (Legacy Stack)

### Logging pipeline:

```text
Docker → Promtail → Loki → Grafana
```

### Metrics pipeline:

```text
Node Exporter → Prometheus → Grafana
Proxmox Exporter → Prometheus → Grafana
```

---

## AFTER (Current Stack)

### Logging pipeline (modernized):

```text
Docker → Grafana Alloy → Loki → Grafana
```

### Metrics pipeline (unchanged but validated):

```text
Node Exporter → Prometheus → Grafana
Proxmox Exporter → Prometheus → Grafana
```

---

# 3. 🧹 COMPONENT CHANGES

## ❌ Removed / Deprecated (conceptually)

* Promtail (replaced by Alloy in design discussions)
* Manual log shippers

## ✅ Retained (critical infrastructure)

* Node Exporter
* Proxmox Exporter
* Prometheus
* Grafana
* Loki

## 🆕 Introduced / Expanded Role

* Grafana Alloy (central telemetry agent)

---

# 4. 📦 DOCKER COMPOSE CHANGES

## Updated services in `docker-compose.yml`

### Added / Expanded:

### 🔹 Grafana Alloy

* Replaces Promtail log shipping role
* Connects directly to:

  * Docker socket
  * Loki write API
* Handles:

  * Log discovery
  * Relabeling
  * Shipping pipeline

---

### 🔹 Loki (validated configuration)

* Switched to **single-node filesystem mode**
* Confirmed:

  * `/ready` endpoint = OK
  * `/labels` endpoint = functional
  * ingestion pipeline active

---

### 🔹 Prometheus

* No structural changes
* Confirmed:

  * `/ready = 200 OK`
  * active targets healthy

---

### 🔹 Grafana

* No changes
* Confirmed ready for Loki queries

---

# 5. 🧾 LOKI CONFIGURATION CHANGES

## Key stabilization change:

### BEFORE (problem state)

* Implicit/unclear distributed config assumptions
* Risk of ring/ingester readiness issues

---

### AFTER (stable state)

Single-node filesystem mode:

* `replication_factor: 1`
* `kvstore: inmemory`
* filesystem-based chunk storage
* compactor configured locally

---

## Result:

* ❌ No more 503 readiness loop
* ✅ Immediate startup health
* ✅ Stable ingestion pipeline

---

# 6. ⚙️ GRAFANA ALLOY IMPLEMENTATION

## Configuration added:

### Core responsibilities:

#### 1. Docker log discovery

```river
discovery.docker
```

#### 2. Log relabeling

```river
discovery.relabel
```

#### 3. Loki log shipping

```river
loki.source.docker
loki.write
```

---

## Observed behavior:

### ✅ Alloy logs confirm:

* Graph evaluation successful
* Docker container discovery active
* Log forwarding working
* Loki write pipeline active

### ⚠ Minor warning:

* Docker container inspection mismatch:

  ```
  No such container: <id>
  ```

  → harmless stale container reference

---

# 7. 📡 PROMETHEUS PIPELINE VALIDATION

## Confirmed working:

### Targets:

* Node Exporter → UP
* Proxmox Exporter → UP

### APIs:

* `/api/v1/status/buildinfo` → OK
* `/api/v1/targets` → healthy

---

# 8. 📊 NODE EXPORTER OBSERVATION

## Issue observed:

```
write: broken pipe
```

## Root cause:

* Prometheus scrape connection closing early
* HTTP client disconnect mid-metric stream

## Classification:

| Type         | Severity        |
| ------------ | --------------- |
| Error        | Non-critical    |
| Impact       | None on metrics |
| Fix required | Optional        |

## Confirmed working:

* `/metrics` endpoint fully functional

---

# 9. 🧪 LOKI QUERY READINESS

## Confirmed working endpoints:

* `/ready` → 200 OK
* `/labels` → returns:

```json
["container", "service_name"]
```

## Query capability:

✔ Fully operational

Example working queries:

```logql
{container=~".+"}
```

```logql
{container="grafana"}
```

```logql
{container=~".+"} |= "error"
```

---

# 10. 📈 FINAL PIPELINE HEALTH

## Metrics pipeline

```text
NODE EXPORTER + PROXMOX EXPORTER
        ↓
     PROMETHEUS
        ↓
      GRAFANA
```

Status: 🟢 HEALTHY

---

## Logging pipeline

```text
DOCKER CONTAINERS
        ↓
   GRAFANA ALLOY
        ↓
       LOKI
        ↓
     GRAFANA
```

Status: 🟢 HEALTHY

---

# 11. 🧾 FINAL STATE SUMMARY

| Layer                   | Status             |
| ----------------------- | ------------------ |
| Compute (VM/LXC)        | ✅ Stable           |
| Metrics (Prometheus)    | ✅ Healthy          |
| Logs (Loki)             | ✅ Stable           |
| Log Agent (Alloy)       | ✅ Working          |
| Visualization (Grafana) | ✅ Ready            |
| Exporters               | ⚠ Minor noise only |

---

# 12. 🧠 KEY ENGINEERING OUTCOMES

## Major improvements achieved:

### 1. Logging modernization

* Promtail → Alloy migration conceptually completed
* Unified log pipeline architecture

### 2. Loki stabilization

* Eliminated distributed-mode complexity
* Forced deterministic single-node behavior

### 3. Observability validation

* Full metrics + logs pipeline verified end-to-end

### 4. Query readiness confirmed

* Loki fully usable in Grafana Explore

---

# 13. ⚠ REMAINING MINOR ITEMS

* Node exporter "broken pipe" logs (cosmetic)
* Stale container ID warning in Alloy logs
* Optional: add alerting layer in Grafana

---

# 14. 🏁 FINAL CONCLUSION

Your homelab telemetry stack is now:

> 🟢 Fully operational
> 🟢 Single-node stable
> 🟢 Query-ready in Grafana
> 🟢 Modern log pipeline (Alloy-based)
> 🟢 SRE-grade observability baseline achieved

---

If you want next step upgrades, I can generate:

* 📊 Grafana SRE dashboard JSON (import-ready)
* 🔔 Alert rules (CPU, disk, Loki errors)
* 🔐 secure auth layer for Loki + Prometheus
* 💾 backup + restore automation
* 📦 GitOps structure for full homelab

Just tell me.
