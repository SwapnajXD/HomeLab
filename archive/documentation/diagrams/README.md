# Historical diagrams

These are the pre-V2 standalone Mermaid diagrams, preserved unchanged. They show retired services and earlier service placement; embedded diagrams elsewhere in the historical archive likewise retain their original context.

`architecture-diagram-legacy.mmd` was previously `docs/architecture-diagram.mmd`. The other `.mmd` files were previously under `architecture/`. `README-pre-v2.md` preserves the old diagram guide, including its original relative references.

Use the [current diagram index](../../../diagrams/README.md) for the September 12 V2 baseline.
